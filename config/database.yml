# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
development:
  adapter: postgresql
  database: Pinterest2_dev
  host: localhost

test:
  adapter: postgresql
  database: Pinterest2_test
  host: localhost
